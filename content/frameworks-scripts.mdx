---
title: Frameworks & Scripts
---

import { Callout } from 'nextra/components'

# Frameworks & Scripts

LACORE is **standalone-first** — it ships with its own identity, jobs and duty system and needs no
framework. But when you already run one, or use popular paid resources, LACORE **integrates with them
automatically**: if the resource is running, the integration turns on; if not, LACORE carries on
standalone. Nothing here is ever required.

## Supported frameworks

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(230px, 1fr))', gap: '12px', margin: '1.25rem 0' }}>

  <div style={{ display: 'flex', alignItems: 'center', gap: '13px', padding: '14px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ width: 46, height: 46, borderRadius: '12px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#1f6feb,#0b3d91)', color: '#fff', fontWeight: 800, fontSize: '15px', flexShrink: 0 }}>LA</div>
    <div style={{ minWidth: 0 }}>
      <div style={{ fontWeight: 700, lineHeight: 1.2 }}>Standalone</div>
      <div style={{ opacity: 0.7, fontSize: '12.5px' }}>LACORE identity</div>
    </div>
    <span style={{ marginLeft: 'auto', fontSize: '11.5px', fontWeight: 700, color: '#1f6feb', whiteSpace: 'nowrap' }}>★ Default</span>
  </div>

  <div style={{ display: 'flex', alignItems: 'center', gap: '13px', padding: '14px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ width: 46, height: 46, borderRadius: '12px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#3a4a5e,#1c2733)', color: '#fff', fontWeight: 800, fontSize: '14px', flexShrink: 0 }}>ESX</div>
    <div style={{ minWidth: 0 }}>
      <div style={{ fontWeight: 700, lineHeight: 1.2 }}>ESX</div>
      <div style={{ opacity: 0.7, fontSize: '12.5px' }}>es_extended</div>
    </div>
    <span style={{ marginLeft: 'auto', fontSize: '11.5px', fontWeight: 700, color: '#16a34a', whiteSpace: 'nowrap' }}>✓ Supported</span>
  </div>

  <div style={{ display: 'flex', alignItems: 'center', gap: '13px', padding: '14px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ width: 46, height: 46, borderRadius: '12px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#8b5cf6,#5b21b6)', color: '#fff', fontWeight: 800, fontSize: '15px', flexShrink: 0 }}>QB</div>
    <div style={{ minWidth: 0 }}>
      <div style={{ fontWeight: 700, lineHeight: 1.2 }}>QBCore</div>
      <div style={{ opacity: 0.7, fontSize: '12.5px' }}>qb-core</div>
    </div>
    <span style={{ marginLeft: 'auto', fontSize: '11.5px', fontWeight: 700, color: '#16a34a', whiteSpace: 'nowrap' }}>✓ Supported</span>
  </div>

  <div style={{ display: 'flex', alignItems: 'center', gap: '13px', padding: '14px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ width: 46, height: 46, borderRadius: '12px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#06b6d4,#0e7490)', color: '#fff', fontWeight: 800, fontSize: '13px', flexShrink: 0 }}>QBX</div>
    <div style={{ minWidth: 0 }}>
      <div style={{ fontWeight: 700, lineHeight: 1.2 }}>QBox</div>
      <div style={{ opacity: 0.7, fontSize: '12.5px' }}>qbx_core</div>
    </div>
    <span style={{ marginLeft: 'auto', fontSize: '11.5px', fontWeight: 700, color: '#16a34a', whiteSpace: 'nowrap' }}>✓ Supported</span>
  </div>

</div>

When a framework is detected, LACORE pulls **jobs and identity** from it:

- **Framework job = duty authorisation.** A player whose framework job maps to an agency can go on
  duty in the MDT / dispatch **without also needing a LACORE Discord duty role** — verified
  server-side, so it can't be spoofed.
- **Instant job sync.** Job changes apply immediately via `esx:setJob` / `esx:playerLoaded` (and the
  QBCore equivalents), not a slow poll.
- **Identity → RP name.** The framework character name becomes the player's LACORE RP name (unless
  they set one via `/char` or multichar, which always wins).
- **Optional auto-duty.** `Bridge.autoDuty` can put mapped jobs on/off duty the moment the framework
  job changes.

Map your framework jobs to agencies in `configs/cfg-bridge-sh.lua` — full API on the
[Framework Bridge](/features/bridge) page.

<Callout type="warning">
  **Load order:** `ensure` your framework **before** `lacore` in `server.cfg` (detection runs at
  startup). LACORE keeps no hard dependency, so it still boots standalone if none is present.
</Callout>

## Supported scripts

Third-party resources LACORE plugs into when installed — configured in
`configs/cfg-integrations-sh.lua` (each toggle `"auto"` = on when present, or `false` to disable).

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: '12px', margin: '1.25rem 0' }}>

  <div style={{ padding: '15px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
      <div style={{ width: 44, height: 44, borderRadius: '11px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#f59e0b,#b45309)', fontSize: '20px', flexShrink: 0 }}>🏠</div>
      <div>
        <div style={{ fontWeight: 700, lineHeight: 1.2 }}>vms_housing</div>
        <div style={{ opacity: 0.7, fontSize: '12px' }}>Native export</div>
      </div>
    </div>
    <div style={{ fontSize: '13px', opacity: 0.85 }}>A queried person's <b>registered property addresses</b> show in the MDT person record.</div>
  </div>

  <div style={{ padding: '15px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
      <div style={{ width: 44, height: 44, borderRadius: '11px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#6d28d9,#4c1d95)', fontSize: '20px', flexShrink: 0 }}>🪪</div>
      <div>
        <div style={{ fontWeight: 700, lineHeight: 1.2 }}>vms_identity</div>
        <div style={{ opacity: 0.7, fontSize: '12px' }}>Via framework bridge</div>
      </div>
    </div>
    <div style={{ fontSize: '13px', opacity: 0.85 }}>Character <b>identity (name)</b> is read automatically for MDT records.</div>
  </div>

  <div style={{ padding: '15px 16px', border: '1px solid rgba(128,128,128,0.22)', borderRadius: '14px', background: 'rgba(128,128,128,0.05)' }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '8px' }}>
      <div style={{ width: 44, height: 44, borderRadius: '11px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'linear-gradient(135deg,#0ea5e9,#0369a1)', fontSize: '20px', flexShrink: 0 }}>👥</div>
      <div>
        <div style={{ fontWeight: 700, lineHeight: 1.2 }}>vms_multichars</div>
        <div style={{ opacity: 0.7, fontSize: '12px' }}>Via framework bridge</div>
      </div>
    </div>
    <div style={{ fontSize: '13px', opacity: 0.85 }}>The <b>active character</b> is recognised; querying an online player with no LACORE profile still returns a record.</div>
  </div>

</div>

<Callout type="info">
  `vms_identity` and `vms_multichars` are framework resources (ESX / QBCore / QBox), so they work
  through LACORE's [framework bridge](/features/bridge) — install a supported framework and they're
  picked up automatically.
</Callout>

<Callout type="info">
  Want another script supported? Ask in our [Discord](https://discord.gg/b6YbvVEvYp) — if it exposes
  a clean export or event, it can usually be bridged the same way.
</Callout>
