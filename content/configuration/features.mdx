---
title: Feature Toggles
---

import { Callout } from 'nextra/components'

# Feature Toggles — `configs/cfg-features-sh.lua`

Run **exactly the parts of LACORE you want**. Want to use *only* the MDT/CAD? Turn everything else off
in one file. Every major component checks this config at load: a disabled feature's module simply
**doesn't load** — no commands, no threads, no events, ~0 ms cost.

<Callout type="info">
  **Default is ON.** A missing or unset key counts as enabled, so leaving this file alone keeps every
  feature exactly as before. Infrastructure (database, Discord, security/anti-cheat, framework bridge,
  identity, profile, location) is **always on** and can't be disabled.
</Callout>

## How it works

Every feature has a key in the `Features` table. A group can be a plain boolean, or a table with an
`enabled` flag plus finer **sub-toggles**. Turning a group off turns all of its sub-features off too.

```lua
Features = {
  -- Police CAD suite — set enabled = false to remove ALL of it,
  -- or trim individual agencies/tools with the sub-toggles.
  cad = {
    enabled  = true,
    lapd     = true,   -- LAPD LACORE Mobile Client
    lasd     = true,   -- LASD CAD / PCMS
    ems      = true,   -- EMS / Fire CAD
    dispatch = true,   -- /dispatch console
    bolo     = true,   -- BOLO system
  },

  phone       = true,  -- LACORE NUI phone
  airunit     = true,  -- Air Unit heli-cam
  cctv        = true,  -- CCTV surveillance
  corrections = true,  -- jail / corrections
  impound     = true,  -- impound lot
  k9          = true,  -- K9 unit
  field       = true,  -- field essentials (breathalyser)
  fingerprint = true,  -- mobile fingerprint ID scanner
  identitylink = true, -- link Discord ⇄ identifiers so the ban network works (no setup)
  nads        = true,  -- New Address System
  stt         = true,  -- push-to-talk radio speech-to-text
  admin       = true,  -- staff tools (Big Brother log + admin menu)
  webdispatch = true,  -- external web dispatcher bridge
  spawnselect = true,  -- map-based spawn picker (false = one fixed spawn)

  civilian = { enabled = true, org = true, turf = true },        -- emotes / props / orgs / turf

  seatbelt     = true, -- /seatbelt, the K bind, the warning icon, windscreen ejection
  speedlimiter = true, -- hold the limiter key in a vehicle to cap its speed
  rpemotes     = true, -- built-in hands-up (/hu, U) + hand-on-holster (/hh, X); off frees U/X for another emote script

  chat = { enabled = true, rp = true, actions = true, leoreq = true, report = true },  -- /me /do /run /leoreq /report …
  aop  = { enabled = true, commands = true },                    -- /aop + /aopvote (commands = false keeps the AOP display only)

  extras = { enabled = true, props = true, stretcher = true,     -- misc gameplay
             trains = true, deathsync = true },
}
```

<Callout type="info">
  Everything defaults to **on**. `rpemotes` gates the built-in `/hu` (U) and `/hh` (X) binds — turn it off
  when another emote / roleplay script owns those keys. `chat` gates the built-in RP chat commands (turn off
  if you run cc-chat / cc-rpchat), and `aop` the Area-of-Play commands.
</Callout>

## Running another HUD that brings its own seatbelt

If a HUD like **JG** — or a standalone seatbelt resource — already provides one, two running at once
means two icons and two sets of rules. Switch LACORE's off:

```lua
Features = {
  seatbelt     = false,   -- /seatbelt is never registered, K stays free for the other script
  speedlimiter = false,   -- only if your HUD also owns cruise control
}
```

With `seatbelt = false` the command is not registered at all, so the other resource can claim it, the
**K** bind is left alone, the warning icon is gone and you are no longer thrown through the windscreen.

<Callout type="info">
  `HudCfg.vehicle` is **not** enough for this — it only hides the display while the mechanic keeps
  running underneath. The limiter is a separate key on purpose, so a HUD that owns cruise control but
  not the seatbelt can take just that one.
</Callout>

## Example: MDT/CAD only

```lua
Features = {
  cad = { enabled = true },   -- keep the police CAD suite …
  phone = false, airunit = false, cctv = false, corrections = false,
  impound = false, k9 = false, field = false, fingerprint = false, nads = false, stt = false,
  admin = false, webdispatch = false, spawnselect = false, rpemotes = false, identitylink = false,
  civilian = { enabled = false },
  chat     = { enabled = false },
  aop      = { enabled = false },
  extras   = { enabled = false },
}
```

<Callout type="warning">
  A resource **restart** is required for changes to apply (the config is read when modules load). The
  file is escrow-open and covered by config backup.
</Callout>
